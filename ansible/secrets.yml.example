# Copy to ansible/secrets.yml (gitignored) and fill in your real values.
# Pass it with:  ansible-playbook ... -e @ansible/secrets.yml
hf_token: "hf_xxxxxxxxxxxxxxxxxxxxxxxx"

# Optional overrides (otherwise the playbook defaults apply):
# parallel: 1
# model_key: qwen3.5-9b
# model_context: 64000
# reload_model: true
